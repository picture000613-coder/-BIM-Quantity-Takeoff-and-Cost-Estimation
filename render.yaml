services:
  - type: web
    name: bim-material-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn render_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
